def solution(A):
    # write your code in Python 3.6

    # # Brute Force
    # for x in range(1, 100000):
    #   if x not in A:
    #     return x

    # More efficient
    sort = sorted(A)
    current = 1
    if sort[len(sort) - 1] <= 1:
      return 1
    for 
    if sort[0] + 1 not in
    



A = [1, 3, 6, 4, 1, 2]
print(solution(A))